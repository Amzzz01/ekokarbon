claude "Make these 3 changes for email verification:

1. lib/adminAuth.js — import sendEmailVerification from 'firebase/auth'. In the register function, after updateProfile, call await sendEmailVerification(credential.user) before returning the user.

2. app/admin/register/page.jsx — add a registered state (useState false). In handleSubmit, after successful register(), instead of router.push('/admin/dashboard'), set registered to true. When registered is true, render a success screen (both mobile and desktop versions) showing a green checkmark icon, title 'Emel Pengesahan Dihantar', message telling user to check their inbox at the registered email address, and a link back to /admin/login.

3. app/admin/login/page.jsx — after successful login in handleSubmit, check if user.emailVerified is false. If not verified, call signOut(auth) and setError with 'Sila sahkan emel anda dahulu. Semak peti masuk anda.' then return early. Import signOut from firebase/auth and auth from @/lib/firebase."